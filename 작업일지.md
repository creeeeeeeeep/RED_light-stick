# 크라운 응원봉 — 작업 기록

마지막 갱신: **2026-08-24**  ·  펌웨어 버전 **0.8.0**

이어서 작업할 때 이 문서부터 읽으면 된다. 아래 순서다.

1. [다시 시작하기](#다시-시작하기) — 환경 띄우는 법
2. [지금 어디까지 됐나](#지금-어디까지-됐나)
3. [다음에 할 일](#다음에-할-일)
4. [구조](#구조)
5. [밟았던 함정](#밟았던-함정) ← 같은 데 두 번 빠지지 않으려고 적어둔 것

---

## 다시 시작하기

세 가지를 띄워야 전체가 돈다.

```
1. 서버      cd <프로젝트 폴더>\server  &&  py server.py
2. 확장      chrome://extensions 에서 새로고침  →  치지직 탭도 F5
3. 봉        전원만 넣으면 알아서 붙는다 (WiFi 설정은 봉이 기억한다)
```

**펌웨어 작업은 `C:\esp\cheerstick`** 에서 한다. 바탕화면 경로에 한글이 있어서
ESP-IDF 도구가 깨진다. 데스크톱의 `firmware/` 는 사본이고 빌드되지 않는다.

```bash
cd C:\esp\cheerstick && .\crown.bat
```

인자 없이 실행하면 대화형 메뉴가 뜬다. 번호를 누르거나 명령을 직접 친다.

| 명령 | 하는 일 |
|---|---|
| `build [-Dev]` | 빌드만 |
| `burn [-Dev]` | 굽기 (시리얼 모니터 안 엶) |
| `flash [-Dev] [-Erase]` | 굽고 모니터까지. `-Erase` 는 설정까지 전부 지움 |
| `monitor [-Dev]` | 굽지 않고 로그만 |
| `release [-Force]` | 배포 빌드를 서버에 올림 (봉들이 OTA로 받아감) |
| `version 0.7.3` | 버전 올리기 → 다시 빌드해야 반영됨 |
| `status` | 어느 빌드가 준비돼 있고 서버에 뭐가 올라가 있는지 |
| `freeport` | COM 포트 잡고 있는 모니터 종료 |

`-Dev` 를 붙이면 **개발자 빌드**다. 모션 로그를 서버로 흘려보내고
(`server/logs/*.csv`) OTA 확인을 5초마다 한다. 배포 빌드는 로그가 없고 6시간마다다.

**확인하는 법.** 확장 팝업을 열면 봉 상태가 뜬다. USB 없이도 보인다.

```
봉: 연결됨  192.168.0.10  v0.7.2  3분째  쌓임 4  ·  LED 600mA
```

버전이 방금 구운 것과 같으면 제대로 들어간 것이다.

---

## 지금 어디까지 됐나

전 구간이 동작한다. **봉 흔들기 → 서버 → 확장 → 치지직 채팅** 이 끝까지 이어진다.

### 동작 확인된 것

- 모션 3종 인식 (좌우 / 위아래 / 격하게) + 8x8 LED 연출
- 흔들면 1초에 하나씩 채팅 입력창에 이모티콘이 쌓인다
- 물리 버튼(GPIO2 또는 내장 BOOT)을 누르면 전송
- WiFi 설정을 봉이 기억한다 — 전원만 넣으면 붙는다
- 설정 페이지에서 WiFi·서버·방코드를 한 번에 넣는다 (Web Serial)
- OTA — `crown release` 하면 봉이 알아서 받아간다
- 개발자 빌드의 모션 로그가 `server/logs/` 에 CSV로 쌓인다
- LED 전력 예산 자동 판별 + 브라운아웃 학습

### 확인 안 된 것 / 미완성

- **임계값 튜닝** — 지금 값은 맨보드를 손으로 흔든 데이터로 맞춘 것이다.
  크라운을 달면 질량과 길이가 달라져 전부 다시 잡아야 한다.
- **OTA 한 바퀴 실제 검증 진행 중** — 2026-08-24 에 v0.8.0 을 서버에 올려두고
  봉이 받아가는지 확인하는 중이다. 서버가 내보내는 것과 받아지는 것까지는
  확인했다(1,037,616 바이트, 빌드와 동일). 봉이 실제로 받아 재부팅하고
  60초 뒤 확정까지 가는지가 남았다.
- **펌웨어 서명 없음** — 지금은 그 URL에서 오는 바이너리면 무엇이든 굽는다.
  팬에게 배포하기 전에 반드시 붙여야 한다. 아래 [다음에 할 일](#다음에-할-일) 참고.
- **서버가 LAN 전용** — `http://192.168.0.10:8787`. 집 밖 봉은 못 붙는다.

---

## 다음에 할 일

### 1. 데모 봉 조립 후 — 조작 방식 변경 (합의됨)

**"버튼을 누르고 있는 동안만 쌓고, 떼면 전송"** 으로 바꾼다.

지금은 봉이 책상에 굴러다니기만 해도 모션으로 잡혀서 채팅창이 지저분해진다.
버튼으로 구간을 명시하면 그게 사라진다.

부수 효과가 둘 있다.

- 치지직 전제("사람 1회 조작 = 1회 전송")에 **더 잘 맞는다.** 누르고 뗀 것은
  경계가 분명하다.
- `QUEUE_ARM_MS`(0.8초 유지) / `QUEUE_GAP_MS`(짧은 공백 무시) 같은 우연한
  움직임 걸러내기 장치가 **대부분 필요 없어진다.** 버튼이 그 역할을 한다.

조립 뒤에 하는 이유는 버튼 위치와 유지 시간이 손에 쥐어 봐야 정해지기 때문이다.

### 2. 조립 후 — 임계값 재튜닝

개발자 빌드를 굽고 실제로 흔들면 `server/logs/crown-xxxx-YYYYMMDD.csv` 에 쌓인다.
USB를 꽂지 않아도 되니 제대로 흔들 수 있다. 그 데이터로 `gesture.c` 의
`gesture_default_config()` 값을 다시 맞춘다.

CSV 열: `time, gesture, v, h, total, peak, gyro, strokes, tilt, ma`

### 3. 배포 전 — 반드시

- **펌웨어 서명** (`CONFIG_SECURE_SIGNED_APPS_NO_SECURE_BOOT`).
  봉에 공개키를 심고 서명 안 맞는 이미지는 굽지 않게 한다. 서버가 뚫리거나
  DNS가 가로채여도 임의 코드가 남의 집 기기에서 돌지 않는다.
  **개인키를 잃으면 그 봉들에는 영영 OTA를 못 한다** — 보관 위치부터 정할 것.
- **Cloudflare Tunnel** — 집 IP를 드러내지 않고 HTTPS 주소를 얻는다.
  포트 포워딩 불필요. 봉 쪽은 인증서 번들이 이미 켜져 있어 준비됐다.
  터널이 없으면 확장이 `ws://` 원격 연결을 못 해서 아예 안 붙는다.
- **`config.json` 의 `admin_token` 변경** — 지금 `change-me` 다.
  이건 아무 방에나 들어갈 수 있는 만능 키다.
- **플래시 암호화 검토** — WiFi 비밀번호가 봉 안에 평문으로 있다.

### 4. 나중에 (급하지 않음)

- 비구독자 폴백 정책 (구독 전용 이모티콘을 못 쓰는 팬)
- 배터리 전압 측정 (GPIO1 을 ADC용으로 비워뒀다)
- ESP Launchpad 같은 웹 플래시 페이지

---

## 구조

### 전체 흐름

```
봉(ESP32-S3)  --HTTP POST-->  서버(aiohttp)  --WebSocket-->  크롬 확장  -->  치지직 채팅
```

**왜 서버를 거치나.** 확장의 서비스워커는 30초 유휴면 종료되는데 WebSocket
통신이 그 타이머를 리셋해 준다. 폴링으로는 봉을 흔들어도 확장이 자고 있을 수 있다.

**왜 봉이 목록을 안 들고 있나.** 진짜 큐는 채팅 입력창이다. 봉이 따로 목록을
보관하면 통신이 한 번 어긋날 때 브라우저와 영구히 다른 상태가 된다. 그래서
봉은 흔들 때마다 쏘고 잊는다. 봉이 세는 숫자는 **LED 밝기 표시용일 뿐**이다.

### 봉 (`C:\esp\cheerstick`)

| 파일 | 역할 |
|---|---|
| `board_config.h` | 핀·타이밍·전력. 튜닝은 대부분 여기서 |
| `gesture.c` | 중력 분해 기반 모션 분류 |
| `led_effect.c` | 연출. 돔 안에서 보이게 설계 |
| `ws2812_matrix.c` | LED 출력 + 전력 상한 |
| `power.c` | 전력 예산 자동 판별 |
| `net.c` | WiFi + 비동기 POST |
| `ota.c` | pull 방식 펌웨어 갱신 |
| `devlog.c` | 모션 로그 스트리밍 (개발자 빌드 전용) |
| `console.c` | 시리얼 명령 |
| `settings.c` | NVS 저장 |

**핀.** LED `GPIO14`, IMU `SDA 11 / SCL 12` (내부 배선, 납땜 불필요).
전송 버튼은 **`GPIO2` + 가운데 둥근 `GND` 패드**. 전원은 오른쪽 위 `5V`+`G`.
`GPIO1` 은 배터리 전압용으로 비워둠. 26~32(플래시), 0/3/45/46(스트래핑)은 금지.

**시리얼 명령** — 설정 페이지가 이걸 대신 눌러준다.
`PING` `SCAN` `WIFI` `ROOM` `TOKEN` `SERVER` `STATUS` `BTN` `LED` `UPDATE` `CLEAR`

- `BTN` — 납땜한 버튼이 붙었는지 확인 (누른 채로 치면 `0`)
- `LED 1200` / `LED auto` — 전력 예산 (설정 페이지에 버튼으로도 있음)

**LED 연출 설계.** 돔이 죽이는 것은 '가는 것'이다 — 한 픽셀 반짝임, 폭 1 막대.
화면 전체를 가로지르는 완만한 그라데이션은 살아남는다. 그래서 두 층으로 나눴다.

- **정보** = 밝기의 리듬(모션) + 밝기의 바닥(쌓인 개수)
- **모양** = 저주파 색 그라데이션

| 모션 | 색 | 리듬 |
|---|---|---|
| IDLE | 흰빛 (미색↔푸른기가 21초에 걸쳐 오감) | 5초에 한 번, 잔잔하게 |
| 좌우 | 자홍~빨강~주황이 좌우로 기움 | 약 1.2초, 숨쉬듯 |
| 위아래 | 무지개 띠가 대각선으로 흐름 | 3.5초에 한 바퀴 |
| 격하게 | 금빛 속 흰 중심이 부풀었다 오므라듦 | 쿵쿵…쿵쿵 (초당 5~7회) |

무지개는 색상환을 **170도만** 깐다. 한 바퀴 다 깔면 돔 안에서 섞여 허옇게 된다.

**대기 상태는 흰빛으로 켜둔다.** 예전 호박색은 너무 어두워 켜졌는지 헷갈렸다
(체감밝기 0.7). 지금은 8.9 로 12배 밝고, 모션들(33)보다는 자연히 얌전하다.
흰색은 세 채널을 다 쓰므로 `matrix_headroom()` 이 알아서 상한을 낮게 잡는다.

**모션에 물드는 속도는 들어올 때만 느리다** (`follow_tau_ms`). 손이 살짝
스치기만 해도 판정이 잠깐 튀는데, 그때마다 화면이 통째로 그 색으로 덮이면
대기 중에 자꾸 깜빡인다. 0.2초쯤 스친 오인식은 46%만 물들었다 사라지고
(예전 89%), 진짜로 흔들면 0.6초 안에 제 색이 다 오른다.
격하게 흔들기는 예외다 — 늦추면 첫 타격의 힘이 죽는다.

**전력.** WS2812B 는 채널마다 내부 정전류가 있지만 **총량을 막는 건 아무것도 없다.**
64픽셀×3채널이면 이론상 3.8A. 총량을 지키는 것은 펌웨어뿐이다.

`power.c` 가 실행 중에 정한다. USB 호스트가 붙어 있으면 PC(600mA), 없으면
충전기(900mA)로 추정한다. 판정은 **부팅 4초 뒤**에 한다 — 그전에는 USB 열거가
안 끝나서 PC도 "호스트 없음"으로 보인다. 전압이 주저앉아 재부팅되면
(`esp_reset_reason`) 한 단계 낮춰 NVS에 적는다. 단계는 400/600/900/1200.

`led_effect.c` 는 그리기 **전에** `matrix_headroom()` 으로 상한을 구해 그 안에서만
움직인다. 나중에 눌리면 밝은 구간이 전부 같은 값에 붙어 밝기 변화가 사라지는데,
돔 안에서는 그게 유일한 단서다.

### 서버 (`server/`)

| 엔드포인트 | 용도 |
|---|---|
| `POST /api/stick` | 봉의 이벤트 (`add`/`send`/`clear`/`status`) |
| `GET /ws` | 확장이 붙는 곳 |
| `GET /api/firmware` | OTA 확인 |
| `POST /api/log` | 모션 로그 수집 |
| `GET /api/status` | 상태 (방 목록은 `admin_token` 필요) |
| `/test` `/monitor` | 봉 없이 테스트 / Web Serial 모니터 |

**방 코드와 토큰.** 팬은 **아무것도 입력하지 않는다.** 설정 페이지가 한 쌍으로
만들어 봉과 확장에 동시에 써넣고, 서버는 그 방을 처음 본 순간 등록한다
(`rooms.json`). 이후 그 방은 그 토큰만 받는다.

방 코드는 화면·로그에 떠도 되는 **이름**이고, 토큰이 **열쇠**다. 그래서 콘솔에는
방 코드를 10자까지만 찍는다 — 이 창이 방송에 잡히면 팬들 방 코드가 나간다.

**OTA.** 봉이 물어보는 pull 방식이라 봉은 아무 포트도 열지 않는다.
**더 새 버전만** 받는다. 되돌리려면 `crown release -Force`.

### 확장 (`extension/`)

| 파일 | 역할 |
|---|---|
| `background.js` | 서버 WebSocket, 대상 탭·프레임 결정 |
| `content.js` | 격리 월드 ↔ 페이지 월드 중계 |
| `inject.js` | 페이지 월드. 피커를 눌러 이모티콘 입력 |
| `setup.html/js` | 설정 마법사 + 상태판 (`options_page`) |
| `popup.html/js` | 서버 설정, 대상 탭 고정, 수동 테스트, 봉 상태 |

**이모티콘 입력 방식.** DOM 직접 삽입은 치지직이 정화해서 안 된다. 피커를 몰래
열고 해당 버튼을 클릭하는 것만 동작한다 (`stealthOn/Off` 로 화면에서 감춤).

**상한 10개.** 치지직 채팅 입력창의 한계다(실측). 봉은 상한과 무관하게 계속
보내고, 넘치는 것은 확장이 조용히 거절한다. 봉이 막으면 카운터가 어긋났을 때
흔들어도 아무 일이 안 일어나는 상태가 된다.

---

## 밟았던 함정

같은 데 두 번 빠지지 않으려고 적어둔다.

### 환경

**PowerShell 5.1 은 BOM 없는 UTF-8 `.ps1` 을 ANSI 로 읽는다.**
한글이 깨지고 파서가 죽는다. `.ps1` 은 반드시 **UTF-8 BOM** 으로 저장.

**한글 경로에서 ESP-IDF 가 깨진다.** 그래서 펌웨어는 `C:\esp\cheerstick`.

**COM 포트는 하나만 잡는다.** 설정 페이지(Web Serial)와 시리얼 모니터가 서로
막는다. `crown freeport` 로 정리.

**`python` 은 스토어 스텁이다.** `py` 를 쓸 것.

### 봉

**서버 코드를 고쳤으면 서버를 재시작해야 한다.** 당연한데 자꾸 잊는다.
`/api/firmware` 응답에 `force` 가 있는지로 새 코드인지 알 수 있다.

**굽기는 NVS 를 안 지운다.** 설정(WiFi·방코드·LED 예산)은 `nvs` 파티션에 따로
살고 굽기는 `0x0/0x8000/0x10000/0x20000` 만 쓴다. 이건 **의도한 동작**이다 —
OTA 때마다 WiFi를 다시 잡으라고 할 순 없다. 지우려면 `flash -Erase`.

**OTA 가 USB 를 되돌린다(고쳤음).** 예전엔 "버전이 다르기만 하면 받는다"라
USB로 새로 구워도 5초 뒤 서버의 옛 버전이 덮어썼다. 지금은 **더 새것만** 받는다.

**USB 호스트 판정을 부팅 즉시 하면 안 된다.** 열거가 안 끝나서 PC도
"호스트 없음"으로 보이고, 그러면 PC 포트에서 충전기용 예산을 잡아 주저앉는다.

**배포 빌드에서 20Hz printf 를 하면 안 된다.** USB 호스트가 없는데
USB-Serial-JTAG 에 계속 밀어 넣으면 버퍼가 차서 쓰기가 밀리고, 그 지연이
200Hz IMU 태스크에 얹히면 제스처 판정이 통째로 밀린다.
그래서 `GESTURE_TUNE_LOG` 는 개발자 빌드 전용이다.

**`GPIO0`(내장 BOOT)을 전송 버튼으로 쓰면 안 된다.** 스트래핑 핀이라 누른 채로
전원이 들어오면 ROM 부트로더로 빠져 먹통처럼 보인다. 그래서 `GPIO2`.

### 제스처

**축 두 개를 축 하나와 비교하면 안 된다.** `rms_h` 는 수평 평면 **두 축**의
합이고 `rms_v` 는 **한 축**이다. 등방 잡음만 있어도 비가 √2(≈1.41)가 나오는데
`axis_ratio` 가 1.35 였다 — **가만히 있어도 좌우로 판정되는 구조**였다.
`rms_h` 를 √2 로 나눠 축 기준으로 맞췄다. 시작 직후 세로 인식률 1% → 84%.

**흔드는 도중에도 판정은 IDLE 로 잠깐씩 빠진다.** 방향을 바꾸는 순간에는
실제로 거의 멈추기 때문이다. 그때마다 처음부터 다시 세면 박자가 끊기고 다시
붙는 순간 하나가 즉시 들어가 두 개가 잇달아 보인다. `QUEUE_GAP_MS`(400ms)
이하의 공백은 이어지는 중으로 본다.

### 이모티콘이 여러 개씩 들어간 것 — 원인이 셋이었다

하나 고쳐도 다음 것이 남아 있어서 계속 재발했다.

1. **`addOnce` 가 200ms 만 기다렸다.** 치지직이 늦게 반영하면 실패로 보고
   다시 클릭 → 같은 명령이 두 번. 500ms 로 늘리고, 기준 개수를 `add()` 시작
   시점에 한 번만 재서 재시도 전에 "늦게 들어왔는지" 확인한다.
2. **모든 프레임에 뿌렸다.** `chrome.tabs.sendMessage(tabId, msg)` 는 frameId 를
   안 주면 탭의 모든 프레임에 배달된다. 입력창처럼 보이는 요소를 가진 프레임이
   둘 이상이면 각자 하나씩 넣는다. `crown-ready` 를 보낸 프레임을 기억해
   거기로만 보낸다. **+** `add()` 가 겹쳐 실행되지 않게 한 줄로 세웠다.
3. **소켓이 둘이었다.** 서비스워커가 죽었다 살아나면 새 인스턴스에서 `ws` 가
   `null` 이라 소켓을 새로 여는데, 서버엔 옛 소켓이 아직 살아 있다.
   확장에 고정 `clientId` 를 주고 **서버가 같은 id 의 옛 소켓을 끊게** 했다.

**그 수정이 재연결 루프를 만들었다(고쳤음).** 옛 소켓의 `onclose` 가 공용 `ws`
참조를 무조건 `null` 로 지워서, 방금 만든 새 소켓까지 날아가고 또 붙고를
반복했다. 핸들러를 자기 소켓에 묶고(`ws !== sock` 이면 무시), 서버가 교체하며
보내는 코드 `4000` 에서는 재시도하지 않게 했다.

### 상태 표시

**캐시를 "방금 받았다"로 도장 찍으면 안 된다.** 서버는 확장이 붙을 때 마지막
봉 상태를 되보내는데, 확장이 그걸 방금 온 것으로 취급해서 **봉이 꺼진 지
몇 시간이 지나도 "켜져 있음"** 으로 보였다. 서버가 `age`(몇 초 전 것인지)를
같이 보내고 확장은 그걸로 계산한다.

### 도구

**heredoc 안의 백슬래시가 망가진다.** `\n` 이 진짜 줄바꿈이 되어 C/Python
문자열이 깨진다. 백슬래시가 들어가는 편집은 Edit 도구를 쓰거나 `chr(92)` 로
조립할 것.

---

## 참고 문서

- `docs/emoji-reference.md` — 치지직 이모티콘 API 구조, 구독 판별
- `docs/문의초안.md` — 치지직에 보낸 문의 (승인 근거)
- `C:\esp\cheerstick\README.md` — 펌웨어 상세
